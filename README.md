# quickbashssldatecheck
Super simple script to check date and subject of a list of SSL sites

# usage

Put your sites in a file, one per line, in format `host:port`

Then `./quickbaskssldatecheck.sh [urlfile]`

